/* Общие настройки страницы */
body {
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #1a1a1a; /* Глубокий графитовый фон */
    color: #ffffff; /* Белый текст */
    display: flex;
    flex-direction: column;
    height: 100vh;
}

/* Сетка: разделение на левую панель и зону сборки */
.app-container {
    display: grid;
    grid-template-columns: 350px 1fr; 
    flex: 1;
    overflow: hidden; /* Чтобы скроллилась только панель, а не весь сайт */
}

/* Левая панель с прокруткой (overflow-y) */
.sidebar {
    background-color: #242424; 
    padding: 20px;
    border-right: 1px solid #333333;
    overflow-y: auto; /* Включает скроллбар, если элементов много */
}

/* Заголовки */
h2, h3 {
    margin-top: 0;
    font-weight: 400;
    letter-spacing: 0.5px;
}

/* Контейнеры выпадающих списков */
.component-select {
    margin-bottom: 20px;
}

.component-select label {
    display: block;
    margin-bottom: 8px;
    font-size: 14px;
    color: #aaaaaa;
}

/* Стилизация выпадающих списков (padding для внутренних отступов) */
.component-select select {
    width: 100%;
    padding: 10px; 
    background-color: #333333;
    color: #ffffff;
    border: 1px solid #444444;
    border-radius: 4px;
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s;
}

.component-select select:focus {
    border-color: #007acc; /* Подсветка при клике */
}

/* Правая панель визуализации */
.visualizer {
    background-color: #1e1e1e;
    padding: 20px;
    display: flex;
    flex-direction: column;
}

/* Пунктирное окно для корпуса, как на рисунке */
#pc-case {
    flex: 1;
    border: 2px dashed #444444; 
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #161616;
}

.placeholder-text {
    color: #555555;
    font-weight: bold;
    letter-spacing: 2px;
}

/* Нижняя панель */
.info-panel {
    background-color: #242424;
    padding: 20px;
    border-top: 1px solid #333333;
}

/* Рамка для инструкций внизу */
#instructions {
    background-color: #1a1a1a;
    border: 1px dashed #555555; 
    padding: 15px;
    border-radius: 6px;
    margin-top: 10px;
    color: #cccccc;
}